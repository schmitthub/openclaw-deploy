#!/usr/bin/env sh

# Auto-fix formatting on staged files (trailing whitespace, newlines, YAML/JSON/TS)
npx lint-staged

# Secret scanning
gitleaks protect --staged --no-banner

# TypeScript type check
npx tsc --noEmit

# Run tests
npx vitest run
